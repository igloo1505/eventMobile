import React from "react";
import { Button } from "react-native-elements";
import Icon from "react-native-vector-icons/FontAwesome";

const NavButton = () => {
  return <Button icon={<Icon name="bars" size={18} color="white" />} />;
};

export default NavButton;
