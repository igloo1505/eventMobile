import React from "react";
import { Button } from "react-native-elements";
import Icon from "react-native-vector-icons/FontAwesome";

const HomeButton = () => {
  return <Button icon={<Icon name="home" size={25} color="white" />} />;
};

export default HomeButton;
